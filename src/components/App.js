import React from "react";
import SearchBar from "./SearchBar/SearchBar";

const App = () => {
    return (
        <div className={"formContainer"}>
            <SearchBar />
        </div>
    );
};

export default App;
